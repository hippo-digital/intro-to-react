
/*
    Task: Wire up a `onClick` handler to fire a message in the alert window.
    A secondary task here is to allow a message to be passed to the component via a prop named `message` which is then
    displayed into the alert
*/

const SimpleButton = () => {
    return <button>Click me!</button>
}

export default SimpleButton
